Recursividade

É quando função chama ela mesma.
Tem que haver uma condição de parada para não gerar loop infinito.

---------------------

Passagem de estrutura de dados por valor como parâmetro

struct {
	
}x;
typedef x carro;

void (carro passeio[]) {
	estrutura
	passeio[i].elemento;
}

===================
Se não usar typedef

struct {
	
}x;
x carro;

void (struct x passeio[]) {
	estrutura
	passeio[i].elemento;
}

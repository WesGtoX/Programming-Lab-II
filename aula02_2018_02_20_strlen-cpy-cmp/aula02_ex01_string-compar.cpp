/*
strlen()
strcpy()
strcmp() 	- função responsável pela comparação de duas strings. Pertencentes a biblioteca string.h
O retorno da função strcmp() pode ser:
zero 		- quando igual a 0 significa que as strings são iguais
positivo	- quando for > 0 significa que a primeira string é maior que a segunda string
negativo	- quando for < 0 significa que a primeira string é menor que a segunda string

Sintaxe:
strcmp(string1, string2);

Exemplo:
if(strcmp(string1, string2) == 0) {
	//instruções para condição verdadeira
}
else {
	//instruções para condição falsa
}

Obs:
	(' ') para caracteres
	(" ") para string
*/

/*
MATRIZ

Uma matriz ou vetor bidimensional é um espaço bidimensional 
para armazenamento de informações de um único tipo de dado.

Para a mnipulação da matriz deve-se informar um par de coordenadas,
a primeira representa o índice horizontal e a segunda representa o
índice vertical (linha, coluna).

Declaração de uma matriz:
tipo_de_dados nome_matriz [linhas][colunas];

*/
